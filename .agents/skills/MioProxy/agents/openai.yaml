interface:
  display_name: "MioProxy"
  short_description: "Repo-specific patterns and workflows for MioProxy"
  default_prompt: "Use the MioProxy repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true